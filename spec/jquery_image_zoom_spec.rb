require 'spec_helper'

describe JqueryImageZoom do
  it 'has a version number' do
    expect(JqueryImageZoom::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
